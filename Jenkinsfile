pipeline {
  agent any
  stages {
    stage('check for files') {
      steps {
        sh 'ls -l'
        sh 'npm install;'
      }
    }

    stage('') {
      steps {
        sh 'ls -l'
      }
    }

  }
}